node_modules/
.DS_Store
Thumbs.db

# Assistant scratch files never belong in a repo, and this one is the profile
# itself. Standing rule: no reference to any assistant, anywhere, in any repo.
# Listed rather than trusted, because the file that caused this was added twice
# before anyone looked at whether it belonged there at all.
.claude/
CLAUDE.md
AGENTS.md
.aider*
.cursor/
